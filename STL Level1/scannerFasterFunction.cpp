#include <bits/stdc++.h>
using namespace std;
//#define optimize() ios_base::sync_with_stdio(0), cin.tie(0), cout.tie(0);
#define optimize() ios_base::sync_with_stdio(0),cin.tie(0),cout.tie(0);
#define endl '\n';
int main()
{
    optimize();
    string s,s1;
    cin>>s;
    cout<<s<<endl;

    getline(cin,s1);
    cout<<s1<<endl;

    return 0;
}